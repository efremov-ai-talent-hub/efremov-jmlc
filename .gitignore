.env
.DS_Store
__pycache__/
*.pyc
CLAUDE.local.md

# Seed audio. Recordings of real calls are personal data — voices, names, phone
# numbers — and git history is permanent. Keep the directory, never its contents.
seeds/audio/*
!seeds/audio/.gitkeep

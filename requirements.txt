# Runtime dependencies of the pipelines package and the domain layer it drives.
# Installed into the Prefect worker image. Pinned per the project rule that
# critical dependencies carry exact versions.
trino==0.337.0
sqlalchemy==2.0.44
openai==2.9.0
pydantic-ai==1.20.0
boto3==1.41.7
pydub==0.25.1
